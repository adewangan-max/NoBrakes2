import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["thumbs.dreamstime.com", "res.cloudinary.com", "img.buzzfeed.com", "images.pexels.com", "images.unsplash.com", "cdn.pixabay.com"],
  },
};

export default nextConfig;
